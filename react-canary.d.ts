/// <reference types="react/canary" />

// `ViewTransition` existe dans le React fourni par l'App Router, mais ses types
// sont publiés à part dans `@types/react/canary`. Cette référence les charge
// pour tout le projet. Voir next.config.ts (`experimental.viewTransition`).
